services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: tecniflow
      POSTGRES_USER: tecniflow
      POSTGRES_PASSWORD: tecniflow
    ports:
      - "5432:5432"
    volumes:
      - tecniflow-postgres:/var/lib/postgresql/data

volumes:
  tecniflow-postgres:
